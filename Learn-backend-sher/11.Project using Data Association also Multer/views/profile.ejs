<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full h-screen p-10 bg-zinc-900 text-white ">
        <div class="w-full flex justify-end ">
        <a class="bg-red-500 py-2 px-3 rounded-md text-smmb-5 inline-block mt-5" href="/logout">Logout</a>
        </div>

        <div class="flex items-start gap-3">

        <div class="w-10 h-10 rounded-md overflow-hidden">
            <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
        </div>

        <h3 class="text-3xl"><span class="font-light">Hello</span>, <%= user.name %> 🤗</h3>

        </div>

        <h5 class="mb-5 mt-3">You can create a new post.</h5>

        <form action="/post" method="post">
            <textarea class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's on your mind" name="content" id=""></textarea>
            <input class="px-3 w-40 py-2 bg-blue-600 block rounded-md mt-2 text-sm" type="submit" value="Create New Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <!-- <div class="post w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-800 bg-zinc-800">
                    <h2 class="text-blue-500 mb-2">@someusername</h2>
                    <p class="text-sm tracking-tight">Lorem ipsum dolor sit amet consectetur adipisicing elit. Quae, eum magnam. A quia voluptates, magnam pariatur reiciendis nihil at minus voluptate soluta delectus culpa asperiores sapiente, molestiae rem autem nobis.</p>

                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="">Like</a>
                        <a class="text-zinc-600" href="">Edit</a>
                    </div>
                </div> -->

                <% user.posts.reverse().forEach(function(post){ %>

<div class="post w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-800 bg-zinc-800">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>

                    <small class="mt-2 inline-block"><%= post.likes.length %></small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-blue-500" href="/like/<%= post.id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>

                <% }) %>
            </div>
        </div>
    </div>
  </body>
</html>